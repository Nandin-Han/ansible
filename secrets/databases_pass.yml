---
# 示例加密变量文件
# 这是一个未加密的示例文件，展示如何组织敏感数据
# 实际使用时应该用 ansible-vault encrypt 加密此文件

# 生产环境变量
production_vars:
  # 数据库连接
  db_connections:
    sg_tongits:
      host: "sg-tongits"
      port: 3306
      database: "tongits"
      username: "ops"
      password: "p1A%dN!R^0Ob8"    
    sg_tongits_log:
      host: "sg-tongits_log-mas"
      port: 3308
      database: "tongits_log"
      username: "ops"
      password: "p1A%dN!R^0Ob8"

    us_tongits:
      host: "us-db-01"
      port: 25678
      database: "tongits"
      username: "ops"
      password: "p1A%dN!R^0Ob8"
    
    us_tongits_log:
      host: "us-stardb-01"
      port: 25678
      database: "tongits_log"
      username: "ops"
      password: "p1A%dN!R^0Ob8"
  
  # Redis 连接
  redis_connection:
    host: "sg-redis"
    port: 6380
    password: "cv6bkkfBtIvIIUz9"


# 加密命令示例：
# ansible-vault encrypt vault/example_secrets.yml
# 
# 使用命令：
# ansible-playbook -i inventory/hosts.yml site.yml --ask-vault-pass
